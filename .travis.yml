language: python
python:
  - "2.6"
  - "2.7"
addons:
  apt:
    packages:
sudo: false
install:
  - pip install setuptools
  - pip install pytest
  - pip install pytest-cov
  - pip install mock
script:
  - python setup.py test
